---
layout: layouts/base
title: "All Tags"
permalink: "/tags/"
---

{% include "std-icons.md" %}
<div style="width: 100%; margin: 0 auto; padding: 1rem; box-sizing: border-box;">
    <h1>All Tags</h1>
    <ul>
        {% if collections.tagslist and collections.tagslist.length > 0 %}
            {% for tag in collections.tagslist %}
                {% set postsWithTag = collections[tag] %}
                <li>
                    <a href="{{ site.baseUrl }}/tags/{{ tag | slug }}">{{ tag }}</a>
                    {% if postsWithTag %}
                        ({{ postsWithTag.length }})
                    {% else %}
                        (0)
                    {% endif %}
                </li>
            {% endfor %}
        {% else %}
            <li>No tags available.</li>
        {% endif %}
    </ul>
</div>
<div style="width: 100%; height: 0; overflow: hidden;">Stupid hack to ensure the page uses the full width of the page
    since I don't know how to fix this otherwise.</div>