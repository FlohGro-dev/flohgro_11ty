---
layout: layouts/base
title: "All Tags"
permalink: "/tags/"
---

<h1>All Tags</h1>

{% set sorted = collections.tagslist | sortedTags(collections) %}
<div class="tag-grid">
{% for item in sorted %}
<a href="{{ site.baseUrl }}/tags/{{ item.tag | slug }}" class="related-post-card tag-card">
  <h3>{{ item.tag }}</h3>
  <span class="tag-count">{{ item.count }} {{ "post" if item.count == 1 else "posts" }}</span>
</a>
{% endfor %}
</div>
